# AGENTS.md

Anleitung für Coding-Agents, die an diesem Repo arbeiten.

## Agent skills

### Issue tracker

Issues und Specs leben als GitHub Issues in diesem Repo (`gh` CLI / GitHub-MCP-Tools). Siehe `docs/agents/issue-tracker.md`.

### Triage labels

Standard-Fünf-Rollen-Vokabular (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). Siehe `docs/agents/triage-labels.md`.

### Domain docs

Single-context Layout: `CONTEXT.md` + `docs/adr/` am Repo-Root. Siehe `docs/agents/domain.md`.
