{
  "name": "waste-connect-v2",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "backend",
    "frontend"
  ],
  "overrides": {
    "rxjs": "7.8.2"
  },
  "scripts": {
    "build": "npm run build -w frontend && node scripts/copy-frontend-build.js && npm run build -w backend",
    "test": "npm test -w backend && npm test -w frontend",
    "start:prod": "npm run build && npm run start:prod -w backend"
  }
}
